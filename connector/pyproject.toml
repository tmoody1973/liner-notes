[project]
name = "datahub-connector-convex"
version = "0.1.0"
description = "Convex -> DataHub ingestion source (recipe-driven). Built in MOO-460."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = []
