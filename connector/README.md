# Convex → DataHub connector

A recipe-driven Python ingestion source that lands Convex deployments' tables,
schemas, and row counts in DataHub — the first Convex connector, built to be
submitted upstream.

**Status:** scaffold only. Implementation is the next Milestone 1 issue (MOO-460).
