{
  "name": "liner-notes",
  "private": true,
  "version": "0.1.0",
  "description": "Radio Milwaukee's airplay history as a DataHub-governed artist-influence knowledge graph, with provenance receipts on every connection.",
  "license": "Apache-2.0",
  "workspaces": [
    "convex",
    "agent",
    "web"
  ],
  "scripts": {
    "seed": "npm --workspace convex run seed",
    "verify:source": "node scripts/verify-source.mjs",
    "steward": "npm --workspace agent run session --",
    "graph": "npm --workspace agent run graph --",
    "editorial": "npm --workspace agent run editorial --",
    "sync:events": "npm --workspace agent run sync:events",
    "governance": "npm --workspace agent run governance"
  }
}
